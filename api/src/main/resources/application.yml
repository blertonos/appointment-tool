spring:
  datasource:
    password: 4DgJEUkZ$!DLS*k
    url: jdbc:postgresql://localhost:5432/AT_DEV
    username: AT_DEV
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true